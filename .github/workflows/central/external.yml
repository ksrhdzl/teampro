name: central-external

on:
  push:
    branches:
      - "main"
    paths:
      - "packages/central/external/**"
    tags:
      - "v*"
  pull_request:
    branches:
      - "main"
    paths:
      - "packages/central/external/**"
  workflow_dispatch:

jobs:
  general:
    uses: ./.github/workflows/central/general.yml

  audit:
    runs-on: ubuntu-latest
    needs: general